#include <ctype.h>
#include "main.h"

/**
 * islower function - checks if an input
 * character is lower or not
 *
 * Return - Returns 1 if true and 0 if false
 *
 */

int _islower(int c)
{
	char i;

	if (islower(i))
		return (1);

	else
		return (0);
}
