#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: value of interger
 * Returns - Absolute value
 * Auth - ALABI TIMOTHY
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}

