#include "main.h"

/**
 * print_array - function that prints n elements of an array of integers,
 * followed by a new line.
 *
 *  @a: int pointer
 *  @n: int pointer (size of Array)
 */

void print_array(int *a, int n)
{
	for (i = 0; i < n; i++)
	{
		_putchar(a[1]);
	}
	_putchar('\n');
}
