#include <stdio.h>
#include <ctype.h>
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 */

int main(void)
{
	char n;

	while (1)
	{
		puctchar(tolower(n));
		putchar('\n');
	}
	return (0);
}
